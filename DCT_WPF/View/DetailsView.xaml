<UserControl x:Class="DCT_WPF.View.DetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DCT_WPF.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             >

    <UserControl.Resources>
        <DataTemplate x:Key="MarketTemplate">
            <StackPanel Orientation="Horizontal" Margin="0,2,0,2">
                <TextBlock Text="{Binding MarketName}" FontWeight="Bold" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding Pair}" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding Price, StringFormat={}{0:F2}$}" Foreground="DarkGreen" Margin="0,0,10,0"/>
                <TextBlock Margin="0,0,10,0">
                    <Hyperlink NavigateUri="{Binding TradeUrl}" RequestNavigate="Hyperlink_RequestNavigate">
                        <TextBlock Text="Trade" Foreground="Blue"/>
                    </Hyperlink>
                </TextBlock>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Background="LightGreen">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0 0 15 0">
            <TextBlock Text="Details Page" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto" 
                      HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding Coins}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="LightBlue" BorderThickness="2" 
                                Margin="10" Padding="0" CornerRadius="8"
                                Background="AliceBlue">
                            <Expander Header="{Binding Name}" 
                                      FontSize="18" 
                                      FontWeight="Bold"
                                      Foreground="DarkBlue"
                                      Background="Transparent"
                                      BorderThickness="0"
                                      Expanded="Expander_Expanded">
                                
                                <Expander.HeaderTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                                <TextBlock Text="{Binding}" 
                                                          FontSize="18" 
                                                          FontWeight="Bold" 
                                                          Foreground="DarkBlue"
                                                          VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </Expander.HeaderTemplate>

                                <StackPanel Margin="15" Background="White" 
                                           Opacity="0.9">

                                    <TextBlock Margin="0,5">
                                        <Run Text="Symbol: " FontWeight="SemiBold" Foreground="DarkSlateGray"/>
                                        <Run Text="{Binding Symbol}" FontWeight="Normal"/>
                                    </TextBlock>

                                    <TextBlock Margin="0,5">
                                        <Run Text="Current price: " FontWeight="SemiBold" Foreground="DarkSlateGray"/>
                                        <Run Text="{Binding CurrentPrice, StringFormat=${0:F2}}" FontWeight="Normal"/>
                                    </TextBlock>

                                    <TextBlock Margin="0,5">
                                        <Run Text="Volume: " FontWeight="SemiBold" Foreground="DarkSlateGray"/>
                                        <Run Text="{Binding TotalVolume, StringFormat=${0:F2}}" FontWeight="Normal"/>
                                    </TextBlock>

                                    <TextBlock Margin="0,5">
                                        <Run Text="Price change in 24h: " FontWeight="SemiBold" Foreground="DarkSlateGray"/>
                                        <Run Text="{Binding PriceChange, StringFormat=${0:F2}}" FontWeight="Normal"/>
                                    </TextBlock>

                                    <ItemsControl x:Name="MarketsList" ItemTemplate="{StaticResource MarketTemplate}" Margin="0,10,0,0"/>
                                </StackPanel>
                            </Expander>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
 
    </Grid>
</UserControl>
