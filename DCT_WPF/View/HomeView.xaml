<UserControl x:Class="DCT_WPF.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DCT_WPF.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0 10 10 50">
            <TextBlock Text="{DynamicResource MainPageTitle}" 
                      FontSize="24" 
                      FontWeight="Bold" 
                      HorizontalAlignment="Center"
                      Foreground="White"/>
            <TextBlock Text="{DynamicResource MainPageSubtitle}" 
                      FontSize="16" 
                      Margin="0,10,0,0" 
                      HorizontalAlignment="Center"
                      Foreground="White"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="15*"/>
                <ColumnDefinition Width="70*"/>
                <ColumnDefinition Width="15*"/>
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Hidden">
                <ItemsControl ItemsSource="{Binding Coins}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="LightBlue" BorderThickness="2"
                                    Margin="10" Padding="15" CornerRadius="8"
                                    Background="AliceBlue">
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontSize="20" FontWeight="Bold"
                                               Foreground="#001fff" Margin="0,0,0,10"/>
                                    <TextBlock Margin="0,2" FontSize="16">
                                        <Run Text="{DynamicResource MainPageSymbolField}" FontWeight="SemiBold"/>
                                        <Run Text="{Binding Symbol}"/>  
                                    </TextBlock>

                                    <TextBlock Margin="0,2" FontSize="16">
                                        <Run Text="{DynamicResource MainPagePriceField}" FontWeight="SemiBold"/>
                                        <Run Text="{Binding CurrentPrice, StringFormat=${0:F2}}"/>  
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>